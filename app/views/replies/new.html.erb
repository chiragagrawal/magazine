<%= form_for    :reply,  :url=> replies_path, :method => :post do |f| %>
    <%= f.label :Reply %>
    <%= f.text_field    :reply %>

    <%#= f.hidden_field :user_id, value: current_user.id %>

    <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>